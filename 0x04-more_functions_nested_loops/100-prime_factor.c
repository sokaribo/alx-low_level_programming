#include <stdio.h>
#include "main.h"

/**
 * main - Prints
 *
 * Return: Always 0
 */
int main(void)
{
	unsigned long int i, n = 612852475143;

	for (i = 2; i < n; i++)
	{
		while (n % i == o)
				n = n / i;
	}
	printf("%lu\n", n);
	return (0);
}
